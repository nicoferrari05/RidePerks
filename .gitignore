# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files (never commit secrets)
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# AI coding tool config — local to this machine, not project source
# (installed via `npx skills add ...`; keeps other repos/collaborators
# from inheriting an unrelated tool's skill library)
/.agents/
/.claude/skills/
/skills-lock.json
